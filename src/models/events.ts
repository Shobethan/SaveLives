// interface for Events
export interface Events {
    eventId: string;
    name: string;
    district: string;
    date: Date;
    contact: string;
    desc: string;
    place: string;
    imageUrl: string;
    ownerId: string;
    imageId: string;
}